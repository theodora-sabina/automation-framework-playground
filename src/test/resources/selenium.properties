# URL of the Selenium Server
#remote.web.driver.url=http://nc-sel-hub-prod-01.int.netcentric.biz:4444/wd/hub
#remote.web.driver.url= http://nc-sel-hub-test-01.int.netcentric.biz:4444/wd/hub
remote.web.driver.url= http://localhost:4444/wd/hub

# Browser instance
browser.instance=${browser.instance}

# Location execution status
remote.status=${remote.status}

# Headless state
is.headless=${is.headless}

# Screenshot path
screenshot.folder.path=/target/extent-report

# Environment of execution
is.env.test=${is.env.test}

# Test Environment Path
env.base.url=${env.base.url}